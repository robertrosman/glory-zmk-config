# Uncomment the following lines to enable the Glory RGB Underglow
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y

# Uncomment the following line to enable the Glory OLED Display
# CONFIG_ZMK_DISPLAY=y

# NOTE: this shared glory.conf is NOT auto-merged by Zephyr (only the built
# shield's own conf, e.g. glory_right.conf, is). Pointing/trackball enablement
# therefore lives in Kconfig.defconfig, and the driver in glory_right.conf.
